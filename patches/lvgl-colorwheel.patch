diff --git a/src/extra/widgets/colorwheel/lv_colorwheel.c b/src/extra/widgets/colorwheel/lv_colorwheel.c
index daf112e..268a251 100644
--- a/src/extra/widgets/colorwheel/lv_colorwheel.c
+++ b/src/extra/widgets/colorwheel/lv_colorwheel.c
@@ -254,7 +254,9 @@ static void draw_disc_grad(lv_event_t * e)
     lv_obj_init_draw_line_dsc(obj, LV_PART_MAIN, &line_dsc);
 
     line_dsc.width = (r * 628 / (256 / LV_CPICKER_DEF_QF)) / 100;
-    line_dsc.width += 2;
+    /*Grow the overlap with the radius: with a fixed +2, integer/trig rounding
+     *opens hairline gaps between neighboring lines on large wheels*/
+    line_dsc.width += 2 + r / 64;
     uint16_t i;
     uint32_t a = 0;
     lv_coord_t cir_w = lv_obj_get_style_arc_width(obj, LV_PART_MAIN);
